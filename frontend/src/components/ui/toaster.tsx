// This component is intentionally minimal since Sonner is already rendered in App.tsx
// It exists only to prevent import errors from older code expecting @/components/ui/toaster

export function Toaster() {
  return null;
}
